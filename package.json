{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^5.2.0",
    "express": "^4.17.1",
    "http": "^0.0.1-security",
    "socket.io": "^2.3.0"
  },
  "scripts": {
    "start": "node app.js",
    "heroku-postbuild": "cd client/ && yarn install && yarn run build"
  },
  "devDependencies": {}
}
